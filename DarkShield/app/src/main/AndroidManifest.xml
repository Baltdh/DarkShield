<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Required for the local security audit to inventory installed packages.
         Google Play distribution has separate package-visibility policy requirements. -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Allows launching Android's uninstall confirmation for an app selected by the user. -->
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <application android:usesCleartextTraffic="false" android:allowBackup="false" android:theme="@style/Theme.DarkShield" android:label="DarkShield" android:icon="@drawable/ic_shield" android:supportsRtl="true">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
